import XCTest
@testable import SwiftConcurrencyExtensions

final class SwiftConcurrencyExtensionsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        // XCTAssertEqual(SwiftConcurrencyExtensions().text, "Hello, World!")
    }
}
